{
  "name": "websocket-pingpong-worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler build",
    "test": "node test.mjs"
  },
  "devDependencies": {
    "wrangler": "^3",
    "ws": "^8"
  }
}
